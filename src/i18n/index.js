import { en } from "./en.js";

// Active language selector (extensible for adding new languages)
export const strings = en;

// Future: add function to switch languages at runtime
// export function setLanguage(lang) { ... }
